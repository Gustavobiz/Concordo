#include <string>
#include <vector>
#include "canal.h"
class Servidor{
    private:
    int IdDono;
    string nome;
    string descrição;
    string codigoConvite;
    std::vector<Canal*> canais;
    std::vector<int> idPart;
    public:
}